;SUBROUTINE        OP       ARG1,ARG2
; $XXXXXX -> adress
; #XX -> decimal (omedelbar)
; ; -> kommentar

; LOAD      DEST,#VAL/$ADR

            HALT
MAIN        LOAD        GR0,#40
            JMP         MAIN
            ADD         GR0,#21
TEST        STORE       GR1,#21
            STORE       GR0,$34
            JMP         TEST
