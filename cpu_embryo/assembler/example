;SUBROUTINE        OP       ARG1,ARG2
; $XXXXXX -> adress
; #XX -> decimal (omedelbar)
; ; -> kommentar

; LOAD      DEST,#VAL/$ADR

            HALT
MAIN        LOAD        GR0,#40
            JMP         MAIN
